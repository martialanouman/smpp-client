# Fail when the Node version does not satisfy `engines`, rather than allowing
# a silent divergence between the workstation and CI.
engine-strict=true

# Unsatisfied peer dependencies are reported, not fatal: the React/ESLint
# ecosystem produces them routinely without real consequence.
strict-peer-dependencies=false
