{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions. Deliberately reduced to the strict minimum (CLAUDE.md §8): no shell, no filesystem access, no network plugin. Every later addition must be justified in review — a capability granted 'for later' is attack surface acquired for nothing in return.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
