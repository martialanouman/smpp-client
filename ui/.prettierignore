# CI step 1 runs `prettier --check .` from this directory. Without these
# exclusions it would fail on files nobody writes by hand.
dist
coverage
node_modules
*.tsbuildinfo

# Directory of IPC types generated by tauri-specta (from milestone 001 on):
# their layout belongs to the generator, not to prettier. Reformatting here
# would produce a permanent diff against CI step 4.
src/ipc/generated
